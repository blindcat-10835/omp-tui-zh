你的 `yield` 已记录；你拥有的 {{count}} 个后台任务{{#if multiple}}s{{/if}}{{#if multiple}}正在{{else}}正在{{/if}}运行：{{jobs}}。

本次运行仅在任务完成后**并且**你提交一个考虑了结果的新 `yield` 后才完成。任务结果作为后续消息到达；你的 `yield` 之后的一个结果会取代它 — 它不会作为最终报告被接受。现在决定：
- 需要结果？等待（`hub` op:"wait"），然后提交一个包含它们的新 `yield`。
- 不再需要任务？取消（`hub` op:"cancel", ids:[…]）；重新 yield。
- 否则请等待；当每个结果到达时，提交一个新的 `yield`（如果结果不影响它，重复报告不变）。